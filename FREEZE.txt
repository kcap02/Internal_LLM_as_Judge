Analysis plan frozen at commit 5a7c8a8fbf1d752e0af23be84f354ba37d4949f0

paper/PREREGISTRATION.md as of that commit is the frozen specification.
The stopping rule in its section 9 applies from this point.

Supersedes 23d9534, whose section 4 described the offset ladder second stage as
RidgeCV on the working residual. The code had used the offset logistic with
out-of-fold AUROC selection since 9d431d3, one day before that freeze, and the
run used the code. Documentation correction, verifiable without reference to
any result. A systematic pass over all 14 quantitative claims found it the only
mismatch; no other text differs.

23d9534 supersedes e6566a8 (cheap-arm third checkpoint named rather than
described by a condition).
