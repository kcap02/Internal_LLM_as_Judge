# Exact environment is recorded in each log header (logs/*.log).
torch
transformers
datasets
accelerate
scikit-learn
numpy
psutil
# PINNED. The pilot ran 0.2.1; 0.2.2 fixed the rw-normalization eigensolver
# dispatch and 0.2.3 adds the hard non-finite-attention assertion (C-NUM).
# Metrics on the sym path are unchanged across these (verified: <=0.02%), but
# a campaign must not straddle library versions silently — the log header
# records the version actually used.
spectral-trust==0.2.3
